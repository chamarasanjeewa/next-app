name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 18.x
        uses: actions/setup-node@v1
        with:
          node-version: 18.x
      - name: Install dependencies
        run: npm install
      - name: Semantic release
        uses: semantic-release/semantic-release@v17
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # Add contents: write permission to the access token
          # permissions: contents: write
      # - name: Release
      #   env:
      #     GITHUB_TOKEN: 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
      #   run: npm run release
